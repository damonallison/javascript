//
// The presence of jsconfig.json indicates that
// this is the root of a JavaScript project.
//
// This file improves the code editing experience
// for JS projects.
//
// By default, the JavaScript Language Service (part of VS code)
// will provide intellisense for all js files found
// in your workspace.
//
// Best Practices:
//
// * Exclude folders with JavaScript that are not part
//   of the source code for your project.
{
    //
    // Note: VS Code will by default exclude the "node_modules" folder.
    // Here, we just make it explicit for illustration purposes.
    //
    "exclude": [
        "node_modules"
    ],

    //
    // By default, type declaration files are automatically downloaded
    // and managed by VS Code for packages listed in project.json
    //
    // You can alternatvely list package to acquire type delcaration files
    // for using `typeAcquisition`.
    //
    "typeAcquisition": {
        "include": [
            "jest"
        ]
    }
}